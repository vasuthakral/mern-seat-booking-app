# ─── Seat Booking Backend — Environment Variables ────────────────────────────
# Copy this file to .env and fill in your values.
# The server reads .env automatically if you use dotenv (see README).

PORT=3001

# PostgreSQL credentials — match your pgAdmin setup
DB_HOST=localhost
DB_PORT=5432
DB_NAME=seat_booking
DB_USER=postgres
DB_PASSWORD=Vasu8854@2004

# Frontend URL for CORS (default: Vite dev server)
FRONTEND_URL=http://localhost:5173
